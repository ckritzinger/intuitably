# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Rants and Epiphanies.
email: humans@intuitably.com
description: >- # this means to ignore newlines until "baseurl:"
  thoughts and rants.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
color_theme: light
footer_text: '<a href="mailto:humans@intuitably.com">Get in touch</a>.'
# twitter_username: jekyllrb
# github_username:  jekyll

# Build settings
# remote_theme (not `theme:`) because GitHub Pages' build is still on the
# legacy pipeline (locked Jekyll 3.10 + a fixed gem allowlist that doesn't
# include type-on-strap) - remote_theme fetches straight from GitHub and is
# on that allowlist. Requires the jekyll-remote-theme plugin below.
remote_theme: sylhare/Type-on-Strap
sass:
  quiet_deps: true # type-on-strap's own scss (and its Font Awesome dep) use old @import/lighten() syntax; not ours to fix
  silence_deprecations: [import] # assets/css/main.scss (root stylesheet) also uses @import
post_navigation: true
paginate: 1000 # effectively unpaginated - few enough posts that a second page just adds friction
paginate_path: "blog/page:num"
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-paginate
  - jekyll-remote-theme

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
